services:
  portfolio:
    image: ghcr.io/acapiv1337/portfolio-website:${IMAGE_TAG:-latest}
    ports:
      - "5174:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    external: true
