services:
  portfolio:
    build: .
    ports:
      - "5174:5173"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - webnet

networks:
  webnet:
    external: true
